# Requirements for optimized S3Vector benchmark

# Core dependencies
numpy>=1.21.0
redis>=4.5.0
asyncio-redis>=0.16.0
aioboto3>=11.0.0
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.28.0
psutil>=5.9.0
tabulate>=0.9.0
lz4>=4.0.0

# Vector processing
scikit-learn>=1.3.0
pyarrow>=12.0.0

# Zarr for chunked array storage
zarr>=2.16.0
numcodecs>=0.11.0

# Optional: FastEmbed for text embeddings
# fastembed>=0.2.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0