[pytest]
asyncio_mode = strict
norecursedirs = bin examples
markers =
    logging_billing: Logging/interception/billing focused test suite.
    live_e2e: Live end-to-end tests requiring an external running stack.
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
