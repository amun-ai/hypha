# =============================================================================
# Hypha Server Kit Secret Configuration
# =============================================================================
# This file contains ONLY secret values (passwords, tokens, keys).
# All other configuration should be set in values.yaml.
# 
# Copy this file to .env and update all values before deploying.
# IMPORTANT: Change ALL passwords and secrets before deploying to production!
# =============================================================================

# -----------------------------------------------------------------------------
# Core Secrets
# -----------------------------------------------------------------------------
# JWT secret for authentication tokens (minimum 32 characters)
HYPHA_JWT_SECRET=change-this-to-a-secure-random-string-at-least-32-chars

# -----------------------------------------------------------------------------
# Database Secrets
# -----------------------------------------------------------------------------
# PostgreSQL password (used by PostgreSQL chart and in connection string)
POSTGRES_PASSWORD=change-this-password

# Database connection URI (update password to match POSTGRES_PASSWORD above)
HYPHA_DATABASE_URI=postgresql+asyncpg://hypha:change-this-password@hypha-server-kit-postgresql:5432/hypha_db

# -----------------------------------------------------------------------------
# Redis Secrets
# -----------------------------------------------------------------------------
# Redis password (used by Redis chart)
REDIS_PASSWORD=change-this-password

# Redis connection URI (update password to match REDIS_PASSWORD above)
HYPHA_REDIS_URI=redis://:change-this-password@hypha-server-kit-redis-master:6379/0

# -----------------------------------------------------------------------------
# S3/MinIO Secrets
# -----------------------------------------------------------------------------
# MinIO/S3 access credentials
HYPHA_ACCESS_KEY_ID=minioadmin
HYPHA_SECRET_ACCESS_KEY=change-this-to-secure-password

# MinIO root credentials (should match above for simplicity)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=change-this-to-secure-password
