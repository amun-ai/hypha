# Hypha Server Kit Environment Variables
# Copy this file to .env and fill in your values, or use ./generate-secrets.sh to auto-generate

# REQUIRED SECRETS
# ================

# Core Hypha Configuration
HYPHA_JWT_SECRET=your-jwt-secret-here

# S3/MinIO Credentials (shared between Hypha and MinIO)
# These credentials will be used by both Hypha server and MinIO
HYPHA_ACCESS_KEY_ID=your-access-key-id
HYPHA_SECRET_ACCESS_KEY=your-secret-access-key

# Database Configuration
# Complete PostgreSQL connection URI with embedded password
HYPHA_DATABASE_URI=postgresql://hypha:your-db-password@hypha-server-kit-postgresql:5432/hypha_db
# Separate password field for PostgreSQL chart configuration (must match password in URI above)
POSTGRES_PASSWORD=your-db-password

# Redis Configuration
# Complete Redis connection URI with embedded password
HYPHA_REDIS_URI=redis://:your-redis-password@hypha-server-kit-redis-master:6379/0
# Separate password field for Redis chart configuration (must match password in URI above)  
REDIS_PASSWORD=your-redis-password

# IMPORTANT NOTES
# ===============
# 1. DO NOT commit this file to version control after filling in real values
# 2. Database and Redis passwords must be the same in both the URI and separate password fields
# 3. S3 credentials are shared between Hypha and MinIO - no separate MinIO credentials needed
# 4. Use ./generate-secrets.sh to automatically generate secure random values for all secrets
# 5. For production, ensure all passwords are strong and unique